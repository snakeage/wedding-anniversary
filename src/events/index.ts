import { annaDmitry } from "@/events/anna-dmitry";

/** The one live demo until public `/[slug]` exists. */
export const demoEvent = annaDmitry;

export function getDemoEvent() {
  return demoEvent;
}
